# Loads the devShell from flake.nix on cd, via direnv + nix-direnv.
# Run `direnv allow` once, after which entering this directory is enough.
use flake

# Export DATABASE_URL etc. from .env if it exists (never commit .env).
dotenv_if_exists .env
